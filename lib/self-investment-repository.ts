import {getStorageMode} from "@/lib/storage/config";import {dbQuery} from "@/lib/storage/postgres";import {getD1Database,makeD1Id} from "@/lib/storage/d1";
export type SelfInvestmentCategory="beauty"|"fashion"|"photo_content"|"learning"|"maintenance"|"other";export type SelfInvestment={id:string;ownerUserId:string;occurredAt:string;category:SelfInvestmentCategory;amount:number;memo?:string;createdAt:string};const entries:SelfInvestment[]=[];const makeId=()=>`selfinv_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,7)}`;type Row={id:string;owner_user_id:string;occurred_at:string;category:SelfInvestmentCategory;amount:number;memo:string|null;created_at:string};const mapRow=(r:Row):SelfInvestment=>({id:r.id,ownerUserId:r.owner_user_id,occurredAt:new Date(r.occurred_at).toISOString(),category:r.category,amount:r.amount,memo:r.memo??undefined,createdAt:new Date(r.created_at).toISOString()});
export async function listSelfInvestments(ownerUserId:string){const mode=getStorageMode();if(mode==="d1"){const db=await getD1Database();if(!db)return[];const result=await db.prepare("SELECT id,owner_user_id,occurred_at,category,amount,memo,created_at FROM velvet_self_investments WHERE owner_user_id=? ORDER BY occurred_at DESC").bind(ownerUserId).all<Row>();return result.results.map(mapRow)}if(mode!=="postgres")return entries.filter(e=>e.ownerUserId===ownerUserId).sort((a,b)=>b.occurredAt.localeCompare(a.occurredAt));const result=await dbQuery<Row>(`select id,owner_user_id,occurred_at::text,category,amount,memo,created_at::text from velvet_self_investments where owner_user_id=$1 order by occurred_at desc`,[ownerUserId]);return result.rows.map(mapRow)}
export async function createSelfInvestment(input:{ownerUserId:string;category:SelfInvestmentCategory;amount:number;occurredAt?:string;memo?:string}){if(!Number.isFinite(input.amount)||input.amount<0)return undefined;const occurredAt=input.occurredAt?new Date(input.occurredAt):new Date();if(!Number.isFinite(occurredAt.getTime()))return undefined;const mode=getStorageMode(),entry:SelfInvestment={id:mode==="d1"?makeD1Id("selfinv"):makeId(),ownerUserId:input.ownerUserId,occurredAt:occurredAt.toISOString(),category:input.category,amount:Math.round(input.amount),memo:input.memo?.trim()||undefined,createdAt:new Date().toISOString()};if(mode==="d1"){const db=await getD1Database();if(!db)throw new Error("D1_NOT_CONFIGURED");await db.prepare("INSERT INTO velvet_self_investments (id,owner_user_id,occurred_at,category,amount,memo,created_at) VALUES (?,?,?,?,?,?,?)").bind(entry.id,entry.ownerUserId,entry.occurredAt,entry.category,entry.amount,entry.memo??null,entry.createdAt).run()}else if(mode!=="postgres")entries.unshift(entry);else await dbQuery(`insert into velvet_self_investments (id,owner_user_id,occurred_at,category,amount,memo,created_at) values ($1,$2,$3,$4,$5,$6,$7)`,[entry.id,entry.ownerUserId,entry.occurredAt,entry.category,entry.amount,entry.memo??null,entry.createdAt]);return entry}
