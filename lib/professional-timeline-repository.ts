import {getStorageMode} from "@/lib/storage/config";import {dbQuery} from "@/lib/storage/postgres";import {getD1Database,makeD1Id} from "@/lib/storage/d1";
export type ProfessionalTimelineItem={id:string;workspaceId:string;userId:string;customerId:string;occurredAt:string;eventType:string;title:string;body?:string;sourceRef?:string};const rows:ProfessionalTimelineItem[]=[];const makeId=()=>`note_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`;type Row={id:string;workspace_id:string;user_id:string;customer_id:string;occurred_at:string;event_type:string;title:string;body:string|null;source_ref:string|null};const map=(r:Row):ProfessionalTimelineItem=>({id:r.id,workspaceId:r.workspace_id,userId:r.user_id,customerId:r.customer_id,occurredAt:new Date(r.occurred_at).toISOString(),eventType:r.event_type,title:r.title,body:r.body??undefined,sourceRef:r.source_ref??undefined});
export async function listProfessionalTimeline(workspaceId:string,userId:string,customerId:string){const mode=getStorageMode();if(mode==="d1"){const db=await getD1Database();if(!db)return[];const r=await db.prepare("select id,workspace_id,user_id,customer_id,occurred_at,event_type,title,body,source_ref from velvet_professional_timeline where workspace_id=? and user_id=? and customer_id=? order by occurred_at desc").bind(workspaceId,userId,customerId).all<Row>();return r.results.map(map)}if(mode!=="postgres")return rows.filter(r=>r.workspaceId===workspaceId&&r.userId===userId&&r.customerId===customerId).sort((a,b)=>b.occurredAt.localeCompare(a.occurredAt));const r=await dbQuery<Row>(`select id,workspace_id,user_id,customer_id,occurred_at::text,event_type,title,body,source_ref from velvet_professional_timeline where workspace_id=$1 and user_id=$2 and customer_id=$3 order by occurred_at desc`,[workspaceId,userId,customerId]);return r.rows.map(map)}
export async function listLatestConversationsByCustomer(workspaceId:string,userId:string,customerIds:string[]){const result=new Map<string,ProfessionalTimelineItem>();if(!customerIds.length)return result;const allowed=new Set(customerIds);if(getStorageMode()==="d1"){for(const id of customerIds){const items=await listProfessionalTimeline(workspaceId,userId,id);const item=items.find(x=>x.eventType==="conversation");if(item)result.set(id,item)}return result}if(getStorageMode()!=="postgres"){rows.filter(r=>r.workspaceId===workspaceId&&r.userId===userId&&r.eventType==="conversation"&&allowed.has(r.customerId)).sort((a,b)=>b.occurredAt.localeCompare(a.occurredAt)).forEach(r=>{if(!result.has(r.customerId))result.set(r.customerId,r)});return result}const r=await dbQuery<Row>(`select distinct on (customer_id) id,workspace_id,user_id,customer_id,occurred_at::text,event_type,title,body,source_ref from velvet_professional_timeline where workspace_id=$1 and user_id=$2 and event_type='conversation' and customer_id=any($3::text[]) order by customer_id,occurred_at desc`,[workspaceId,userId,customerIds]);return new Map(r.rows.map(x=>{const i=map(x);return[i.customerId,i] as const}))}
export async function addProfessionalTimelineItem(input:{workspaceId:string;userId:string;customerId:string;eventType:string;title:string;body?:string;sourceRef?:string;occurredAt?:string}){const item:ProfessionalTimelineItem={id:getStorageMode()==="d1"?makeD1Id("timeline"):makeId(),workspaceId:input.workspaceId,userId:input.userId,customerId:input.customerId,occurredAt:input.occurredAt??new Date().toISOString(),eventType:input.eventType,title:input.title,body:input.body,sourceRef:input.sourceRef};if(getStorageMode()==="d1"){const db=await getD1Database();if(!db)throw new Error("D1_NOT_CONFIGURED");await db.prepare("insert into velvet_professional_timeline(id,workspace_id,user_id,customer_id,occurred_at,event_type,title,body,source_ref) values(?,?,?,?,?,?,?,?,?)").bind(item.id,item.workspaceId,item.userId,item.customerId,item.occurredAt,item.eventType,item.title,item.body??null,item.sourceRef??null).run();return item}if(getStorageMode()!=="postgres"){rows.unshift(item);return item}await dbQuery(`insert into velvet_professional_timeline(id,workspace_id,user_id,customer_id,occurred_at,event_type,title,body,source_ref) values($1,$2,$3,$4,$5,$6,$7,$8,$9)`,[item.id,item.workspaceId,item.userId,item.customerId,item.occurredAt,item.eventType,item.title,item.body??null,item.sourceRef??null]);return item}
