import { createTraceContext, traceHeaders, type TraceContext } from "@/lib/observability";
import { rememberGroups } from "@/lib/remember-fields";
export type AiPlatformStatus={configured:boolean;endpoint?:string;contractReady:boolean;gatewayPath:string;clientConfigured:boolean};
export type CaptureCandidate={type:"knowledge"|"preference"|"next_topic"|"schedule"|"gift"|"memory_field"|"unknown";value:string;fieldLabel?:string};
export type StructuredCaptureResult={status:"success"|"warning";mode:"ai"|"local";candidates:CaptureCandidate[];trace:TraceContext;activityId?:string;errorCode?:string};export type SearchIntentResult={status:"success"|"warning";mode:"ai"|"local";terms:string[];trace:TraceContext;activityId?:string;errorCode?:string};
function baseUrl(){return process.env.AI_PLATFORM_CORE_URL?.trim().replace(/\/$/,"")}function gatewayPath(){return process.env.AI_PLATFORM_CORE_GATEWAY_PATH?.trim()||"/v1/gateway/run"}function clientId(){return process.env.AI_PLATFORM_CORE_CLIENT_ID?.trim()}export function getAiPlatformStatus():AiPlatformStatus{const endpoint=baseUrl();return{configured:Boolean(endpoint),endpoint:endpoint||undefined,contractReady:true,gatewayPath:gatewayPath(),clientConfigured:Boolean(clientId())}}function gatewayEndpoint(){const base=baseUrl();if(!base)return undefined;const path=gatewayPath();return`${base}${path.startsWith("/")?path:`/${path}`}`}
const memoryLabels=rememberGroups.flatMap(group=>group.fields.map(field=>field.label));const memoryLabelSet=new Set(memoryLabels);
export function organizeCaptureLocally(raw:string):CaptureCandidate[]{const values=raw.split(/[。\n]+/).flatMap(value=>value.split(/[、,]/)).map(value=>value.trim()).filter(Boolean);return values.map(value=>{const colon=value.indexOf("：");if(colon>0){const fieldLabel=value.slice(0,colon).trim(),content=value.slice(colon+1).trim();if(memoryLabelSet.has(fieldLabel)&&content)return{type:"memory_field",fieldLabel,value:content}}if(/次回|今度.*聞|聞いてみ|確認する|どうだった.*聞/.test(value))return{type:"next_topic",value};if(/好き|ハマって|好み|よく飲む|苦手|嫌い/.test(value))return{type:"preference",value};if(/もらった|貰った|あげた|プレゼント|お土産/.test(value))return{type:"gift",value};if(/(?:予定に|スケジュールに|リマインド|予約|来店予定|出勤予定)/.test(value)&&/(?:明日|来週|来月|\d{1,2}[月\/\-]\d{1,2}|\d{1,2}日|\d{1,2}:\d{2})/.test(value))return{type:"schedule",value};return{type:"knowledge",value}})}
function localSearchTerms(raw:string){return Array.from(new Set(raw.normalize("NFKC").replace(/[、。,.!?！？/・]/g," ").split(/\s+|好き|で|の|人|客|お客様|探して|教えて/).map(value=>value.trim()).filter(Boolean)))}function safeJsonFromText(text:unknown):unknown{if(typeof text!=="string")return undefined;const trimmed=text.trim().replace(/^```(?:json)?\s*/i,"").replace(/\s*```$/,"");try{return JSON.parse(trimmed)}catch{return undefined}}
async function runGateway(input:{capability:"velvet.capture.structure"|"velvet.search.parse_intent";permission:"velvet.capture.structure"|"velvet.search.parse_intent";ownerUserId:string;workspaceId?:string;goal:string;payload:Record<string,unknown>;prompt:string;trace:TraceContext}){const endpoint=gatewayEndpoint(),client=clientId();if(!endpoint||!client)return undefined;const response=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json",...traceHeaders(input.trace)},cache:"no-store",body:JSON.stringify({auth:{clientId:client,permissions:[input.permission]},activity:{client,...(input.workspaceId?{workspaceId:input.workspaceId}:{}),userId:input.ownerUserId,ownerUserId:input.ownerUserId,capability:input.capability,workflow:"velvet",goal:input.goal,context:{app:"Velvet",sourceApp:"velvet"},input:input.payload},messages:[{role:"system",content:"Return JSON only. Do not add prose or markdown fences."},{role:"user",content:input.prompt}]})});if(!response.ok)throw new Error(`AI Platform Core returned ${response.status}`);const body=await response.json() as{ok?:boolean;result?:{activityId?:string;output?:Record<string,unknown>}};if(!body.ok||!body.result)throw new Error("Invalid gateway response");return{activityId:body.result.activityId,parsed:safeJsonFromText(body.result.output?.text)}}
export async function structureCapture(raw:string,ownerUserId:string,traceSeed?:Partial<TraceContext>):Promise<StructuredCaptureResult>{const trace=createTraceContext(traceSeed);if(!gatewayEndpoint()||!clientId())return{status:"warning",mode:"local",candidates:organizeCaptureLocally(raw),trace,errorCode:"AI_CAPABILITY_NOT_CONFIGURED"};try{const result=await runGateway({capability:"velvet.capture.structure",permission:"velvet.capture.structure",ownerUserId,goal:"Structure one user-entered Velvet contact note into candidate records without mutating Velvet data.",payload:{rawText:raw,allowedMemoryFields:memoryLabels},prompt:`Classify this Velvet contact note into JSON {"candidates":[{"type":"memory_field|knowledge|preference|next_topic|schedule|gift|unknown","value":"...","fieldLabel":"...optional"}]}. If a fact clearly belongs to exactly one allowed memory field, use type memory_field and fieldLabel MUST be one of: ${JSON.stringify(memoryLabels)}. value must contain only the field value, not the label. Examples: "会社を経営している" => fieldLabel "仕事", value "会社経営"; "娘がいる" => "家族", "娘がいる"; "ロレックスをしていた" => "時計", "ロレックス". Do not force ambiguous facts into a field. preference=likes/dislikes that do not fit a more exact field; next_topic=explicit next conversation topic; schedule=ONLY user's appointment/reminder; gift=given/received gift. Preserve meaning, do not invent facts. Note: ${JSON.stringify(raw)}`,trace});const parsed=result?.parsed as{candidates?:unknown}|undefined;const candidates=Array.isArray(parsed?.candidates)?parsed.candidates.flatMap(item=>{if(!item||typeof item!=="object")return[];const record=item as Record<string,unknown>;const type=String(record.type) as CaptureCandidate["type"];if(!["memory_field","knowledge","preference","next_topic","schedule","gift","unknown"].includes(type)||typeof record.value!=="string")return[];if(type==="memory_field"){const fieldLabel=typeof record.fieldLabel==="string"?record.fieldLabel:"";if(!memoryLabelSet.has(fieldLabel))return[{type:"knowledge" as const,value:record.value}];return[{type,value:record.value,fieldLabel}]}return[{type,value:record.value}]}) :[];if(!candidates.length)throw new Error("No candidates");return{status:"success",mode:"ai",candidates,trace,activityId:result?.activityId}}catch{return{status:"warning",mode:"local",candidates:organizeCaptureLocally(raw),trace,errorCode:"AI_UPSTREAM_FALLBACK"}}}
export async function parseSearchIntent(raw:string,ownerUserId:string,traceSeed?:Partial<TraceContext>):Promise<SearchIntentResult>{const trace=createTraceContext(traceSeed);if(!gatewayEndpoint()||!clientId())return{status:"warning",mode:"local",terms:localSearchTerms(raw),trace,errorCode:"AI_CAPABILITY_NOT_CONFIGURED"};try{const result=await runGateway({capability:"velvet.search.parse_intent",permission:"velvet.search.parse_intent",ownerUserId,goal:"Parse a user-triggered Velvet search phrase into local search terms only.",payload:{query:raw},prompt:`Convert this Velvet search into JSON only: {"terms":["term1","term2"]}. Remove particles and generic words such as 人/客/探して. Do not infer facts. Query: ${JSON.stringify(raw)}`,trace});const parsed=result?.parsed as{terms?:unknown}|undefined;const terms=Array.isArray(parsed?.terms)?parsed.terms.map(String).map(v=>v.trim()).filter(Boolean):[];if(!terms.length)throw new Error("No terms");return{status:"success",mode:"ai",terms,trace,activityId:result?.activityId}}catch{return{status:"warning",mode:"local",terms:localSearchTerms(raw),trace,errorCode:"AI_UPSTREAM_FALLBACK"}}
}
