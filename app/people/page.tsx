import Link from "next/link";
import { BottomNav } from "@/components/bottom-nav";
import { getRequestIdentity } from "@/lib/auth/request-identity";
import { listCustomerMemories } from "@/lib/customer-memory-repository";
import { listGrowthCustomers } from "@/lib/growth-engine-customer";
import { listLatestConversationsByCustomer } from "@/lib/professional-timeline-repository";
import { parseNextTopics } from "@/lib/next-topics";
export default async function PeoplePage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;const query=q.trim().toLowerCase();const {workspaceId,userId}=await getRequestIdentity();const [customers,memories]=await Promise.all([listGrowthCustomers(workspaceId,userId),listCustomerMemories(workspaceId,userId)]);const memoryByCustomer=new Map(memories.map(m=>[m.customerId,m]));const customerById=new Map(customers.map(c=>[c.customerId,c]));const ids=new Set([...customers.map(c=>c.customerId),...memories.map(m=>m.customerId)]);const latest=await listLatestConversationsByCustomer(workspaceId,userId,[...ids]);const rows=[...ids].map(customerId=>{const customer=customerById.get(customerId),memory=memoryByCustomer.get(customerId),conversation=latest.get(customerId);return{customerId,displayName:customer?.displayName??memory?.displayNameSnapshot??"お客様",tags:memory?.tags??[],pinned:memory?.pinned??false,memoryText:[memory?.personalityNote,memory?.preferenceNote,memory?.cautionNote,memory?.conversationSummary,memory?.lastInteractionSummary,memory?.nextTopicHint,conversation?.body,conversation?.title].filter(Boolean).join(" ")}}).sort((a,b)=>Number(b.pinned)-Number(a.pinned)||a.displayName.localeCompare(b.displayName,"ja"));const filtered=query?rows.filter(row=>[row.displayName,...row.tags,row.memoryText].join(" ").toLowerCase().includes(query)):rows;return <main className="shell"><header className="header"><div className="brand">お客様</div><Link className="subtle" href="/add">＋ 追加</Link></header><form action="/people" method="get"><input className="searchBox" name="q" defaultValue={q} placeholder="名前・特徴・趣味・前回の話など" autoComplete="off"/></form><div className="row"><div className="sectionTitle">{query?`${filtered.length}件見つかりました`:`${filtered.length}人`}</div><Link className="subtle" href="/search?q=%E3%82%B4%E3%83%AB%E3%83%95&natural=1">文章で探す ›</Link></div>{!query&&<div className="card noticeCard"><div className="formHint">名前を忘れても大丈夫</div><div className="timelineBody">「ゴルフが好きでロレックスの人」のような覚えている情報でも探せます。</div><Link className="secondaryButton actionLink compactForm" href="/search?q=%E3%82%B4%E3%83%AB%E3%83%95%E3%81%8C%E5%A5%BD%E3%81%8D%E3%81%A7%E3%83%AD%E3%83%AC%E3%83%83%E3%82%AF%E3%82%B9%E3%81%AE%E4%BA%BA&natural=1">文章でお客様を探す</Link></div>}<div className="stack">{filtered.map(row=><Link className="card personRow customerOpenRow" href={`/people/${row.customerId}`} key={row.customerId}><div className="avatar">{row.displayName.slice(0,1)}</div><div className="personMain"><div className="personName">{row.pinned?"★ ":""}{row.displayName}</div>{row.tags.length>0&&<div className="personMeta">{row.tags.slice(0,4).join(" · ")}</div>}<div className="formHint">情報一覧・履歴を見る</div></div><span>›</span></Link>)}{filtered.length===0&&<div className="card empty">該当するお客様はいません</div>}</div><BottomNav/></main>}
