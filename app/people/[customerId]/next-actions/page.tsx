import Link from "next/link";import {BottomNav} from "@/components/bottom-nav";import {getRequestIdentity} from "@/lib/auth/request-identity";import {getGrowthCustomerDisplay} from "@/lib/growth-engine-customer";import {listNextActions} from "@/lib/professional-next-action-repository";import {completeNextActionForm,createNextActionForm} from "./actions";
export default async function Page({params,searchParams}:{params:Promise<{customerId:string}>;searchParams:Promise<{saved?:string;error?:string}>}){const {customerId}=await params;const query=await searchParams;const {workspaceId,userId}=await getRequestIdentity();const [customer,actions]=await Promise.all([getGrowthCustomerDisplay({workspaceId,userId,customerId}),listNextActions(workspaceId,userId,customerId)]);const open=actions.filter(a=>a.status==="open"),done=actions.filter(a=>a.status==="done");return <main className="shell"><header className="header"><Link className="subtle" href={`/people/${customerId}`}>‹ 戻る</Link><span className="subtle">{customer.displayName??"お客様"}</span></header><section className="hero"><h1>次回アクション</h1><p>次に会った時に忘れたくないことだけ残します。</p></section>{query.saved&&<div className="card successCard">追加しました</div>}{query.error&&<div className="formError">内容を入力してください。</div>}<form action={createNextActionForm.bind(null,customerId)} className="stack compactForm"><input className="searchBox" name="text" placeholder="例：旅行の感想を聞く" autoComplete="off"/><button className="primaryButton" type="submit">追加する</button></form><div className="sectionTitle">次にすること</div><div className="stack">{open.map(item=><div className="card row" key={item.id}><strong>{item.text}</strong><form action={completeNextActionForm.bind(null,customerId,item.id)}><button className="secondaryButton compactButton" type="submit">完了</button></form></div>)}{open.length===0&&<div className="card empty">次回アクションはありません</div>}</div>{done.length>0&&<details className="detailsCard"><summary>完了したもの（{done.length}件）</summary><div className="stack detailsBody">{done.map(item=><div className="timelineBody" key={item.id}>✓ {item.text}</div>)}</div></details>}<BottomNav/></main>}
