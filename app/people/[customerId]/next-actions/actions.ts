"use server";import {revalidatePath} from "next/cache";import {redirect} from "next/navigation";import {getRequestIdentity} from "@/lib/auth/request-identity";import {completeNextAction,createNextAction} from "@/lib/professional-next-action-repository";
export async function createNextActionForm(customerId:string,formData:FormData){const text=String(formData.get("text")??"").trim();if(!text)redirect(`/people/${customerId}/next-actions?error=empty`);const {workspaceId,userId}=await getRequestIdentity();await createNextAction(workspaceId,userId,customerId,text);revalidatePath(`/people/${customerId}`);revalidatePath(`/people/${customerId}/next-actions`);redirect(`/people/${customerId}/next-actions?saved=1`)}
export async function completeNextActionForm(customerId:string,id:string){const {workspaceId,userId}=await getRequestIdentity();await completeNextAction(workspaceId,userId,customerId,id);revalidatePath(`/people/${customerId}`);revalidatePath(`/people/${customerId}/next-actions`)}
