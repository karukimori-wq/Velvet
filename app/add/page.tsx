import Link from "next/link";
import { BottomNav } from "@/components/bottom-nav";
import { createCustomerFromVelvetAction } from "./actions";

export default async function AddPage({searchParams}:{searchParams:Promise<{error?:string;name?:string}>}){const {error,name}=await searchParams;return <main className="shell"><header className="header"><div className="brand">追加</div></header><section className="hero"><h1>何を残しますか？</h1><p>初めて会った人と、いつものお客様で入口を分けています。</p></section>
 <div className="card"><div className="timelineTitle">初めてのお客様</div><div className="timelineBody">まず名前だけ登録します。外見や好みは次の画面で、分かったところだけ残せます。</div><form action={createCustomerFromVelvetAction} className="stack compactForm"><input className="searchBox" name="displayName" placeholder="名前・呼び名" defaultValue={name??""} autoComplete="off"/>{error==="name"&&<div className="formError">名前か呼び名を入れてください。</div>}{error==="customer_create"&&<div className="formError">今はお客様を登録できません。少し時間をおいて再度お試しください。</div>}<button className="primaryButton" type="submit">名前を登録して、この人を覚える</button></form></div>
 <div className="sectionTitle">登録済みのお客様</div><div className="stack registerHub"><Link className="card registerChoice" href="/capture"><div className="registerIcon">話</div><div><strong>今日の接客を残す</strong><p>前回を確認して、今日話したことを記録</p></div><span>›</span></Link><Link className="card registerChoice" href="/remember"><div className="registerIcon">人</div><div><strong>この人について追加</strong><p>外見・持ち物・人となり・好みなど</p></div><span>›</span></Link></div>
 <div className="sectionTitle">その他</div><div className="quickRegisterGrid"><Link className="card quickRegister" href="/schedule">予定を追加</Link><Link className="card quickRegister" href="/self-investment">自分の記録</Link></div><BottomNav/></main>}
