import Link from "next/link";import { BottomNav } from "@/components/bottom-nav";import { CaptureChatInput } from "@/components/capture-chat-input";import { getRequestIdentity } from "@/lib/auth/request-identity";import { getGrowthCustomer,listGrowthCustomers } from "@/lib/growth-engine-customer";import { getCustomerMemory } from "@/lib/customer-memory-repository";import { listCaptures } from "@/lib/capture-repository";import { buildCustomerRecall } from "@/lib/customer-recall";import { rememberGroups } from "@/lib/remember-fields";import { organizeCaptureAction } from "./organize/actions";
export default async function CapturePage({searchParams}:{searchParams:Promise<{customerId?:string;saved?:string;error?:string;fromVisit?:string}>}){const {customerId,saved,error,fromVisit}=await searchParams;const {workspaceId,userId}=await getRequestIdentity();if(!customerId){const customers=await listGrowthCustomers(workspaceId,userId);return <main className="shell"><header className="header"><Link className="subtle" href="/add">‹ 追加</Link><div className="brand">今日の接客</div></header><section className="hero"><h1>誰との話を残しますか？</h1></section><div className="stack">{customers.map(c=>{const name=c.displayName??"お客様";return <Link className="card personRow" href={`/capture?customerId=${encodeURIComponent(c.customerId)}`} key={c.customerId}><div className="avatar">{name.slice(0,1)}</div><div className="personMain"><div className="personName">{name}</div></div><span>›</span></Link>})}{customers.length===0&&<div className="card empty">登録済みのお客様がいません</div>}</div><BottomNav/></main>}const [customer,memory,captures]=await Promise.all([getGrowthCustomer(workspaceId,userId,customerId),getCustomerMemory(workspaceId,userId,customerId),listCaptures(workspaceId,userId,customerId)]);const displayName=customer?.displayName??memory?.displayNameSnapshot??"お客様";const recall=buildCustomerRecall(memory,{maxItems:4,maxNextTopics:2,captures});const phase:"first"|"repeat"=((memory?.tags?.length??0)===0&&!memory?.lastInteractionSummary)?"first":"repeat";return <main className="shell"><header className="header"><Link className="subtle" href={`/people/${encodeURIComponent(customerId)}`}>‹ {displayName}</Link><span className="subtle">今日の接客</span></header><section className="hero"><h1>{phase==="first"?"この人を覚える":"今日、何を残しますか？"}</h1></section>{phase==="repeat"&&(recall.items.length>0||recall.nextTopics.length>0)&&<details className="detailsCard" open={!fromVisit}><summary>前回を確認</summary><div className="stack detailsBody">{recall.items.map(item=><div key={`${item.label}-${item.value}`}><div className="formHint">{item.label}{item.freshness==="aging"?" · 少し前":""}</div><div className="timelineBody">{item.value}</div></div>)}{recall.nextTopics.length>0&&<div><div className="formHint">次に話す</div>{recall.nextTopics.map(topic=><div className="timelineBody" key={topic}>・{topic}</div>)}</div>}</div></details>}{saved&&<div className="card successCard"><strong>保存しました</strong><div className="formHint">{saved}</div></div>}{error==="organize_missing"?<div className="card noticeCard"><strong>入力内容をもう一度確認してください</strong><div className="formHint">一時保存が切れたため、もう一度入力して「整理して確認」を押してください。</div></div>:error&&<div className="formError">入力内容を確認してください。</div>}<form action={organizeCaptureAction.bind(null,customerId,fromVisit)} className="stack"><CaptureChatInput groups={rememberGroups} knownTags={memory?.tags??[]} phase={phase} placeholder={phase==="first"?"見た目、仕事、趣味、好みなど。分かったことをそのまま入力。":"今日話したこと、変わったこと、気づいたことを入力。"}/><button className="primaryButton" type="submit">整理して確認</button></form><Link className="secondaryButton actionLink" href={`/remember?customerId=${encodeURIComponent(customerId)}`}>項目から入力</Link><BottomNav/></main>}
