# Velvet Agent Instructions

Before substantial coding, persistence, authentication, integration, AI capability, deployment, or production-readiness work, consult `karukimori-wq/ai-development-intelligence` using its task retrieval protocol, compact ranking/index, and `memory/velvet/`.

Pay particular attention to runtime readiness, Postgres configuration resolution, canonical ownership boundaries, and prior deployment/persistence evidence.

## Operational learning

For substantial tasks, follow `core/operational-learning-loop.md`. Use a non-sensitive `taskRunId` to correlate retrieval/use/rejection/extraction events when capture is available. Verify current runtime/code before applying remembered deployment or persistence guidance. Apply `core/failure-outcome-attribution.md` before recording prevention or recurrence.

## Authority

Current Velvet code is authoritative for implementation. `professional-platform-contracts` is authoritative for formal platform contracts. Development intelligence is advisory and must be checked against current reality.

## After meaningful work

Run knowledge extraction and capture only reusable discoveries, failures/root causes, decisions, patterns, contradictions, or stable evidence. Prefer updating existing knowledge over creating duplicates.

Never store passwords, connection strings, integration secrets, tokens, credentials, or personal data in development intelligence.